name: "💎 style"

on:
    push:
        branches:
            - main
    pull_request:

jobs:
    style:
        name: "💎 style"
        runs-on: ubuntu-latest
        steps:
            - name: "📚 checkout"
              uses: actions/checkout@v2
            - name: "🟢 node"
              uses: actions/setup-node@v2
              with:
                  node-version: 20
            - name: "📦 install"
              run: npm install
            - name: "🧶 lint"
              run: npm run lint
            - name: "💾 format"
              run: npm run format
